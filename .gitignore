.DS_Store
data/private/
scripts/appsscript/.clasp.json

# Canopy — precise property geometry, never commit
*_garden_extent.geojson
*_parcel.geojson
data/private/

# Generated audit report (self-scanned; never commit)
AUDIT-VIC-ASSUMPTIONS.txt

# v2 area-audit worksheet — contains address strings, never commit
area-audit.csv
